# Quarantined imported rulesets

Directories here are inactive historical snapshots retained only for audit and
rollback. The builder must never load ruleset data from this directory.

- `wh40k-11e-vflam-pre-refresh-2026-09-07/` is the complete import that was
  active immediately after checkpoint commit `c49cf0f` and before the full
  BSData system refresh.
- The active 11e source is always `data/rulesets/wh40k-11e-vflam/`.
